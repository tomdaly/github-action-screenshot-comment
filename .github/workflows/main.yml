name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
      - usess: actions/checkout@v1
      - users: ./action-a
        with:
          MY_NAME: "Tom"
